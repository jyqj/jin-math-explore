# CP-ERR-0006 独立审查票据（未执行）

候选是本轮 PR 最终冻结 head 的12文件目录；准确 head/tree 在 #81 和 PR 交接中。
artifact-sha256.json 绑定其余11文件。必须以新上下文、新 run_id、independent_verifier
角色审查，不继承 solver 对话，不修改候选。本票据没有启动 worker 或出具 receipt。

V1. 逐字核对 WBV-G8 量词。权为模数无关的有界复权，可随 N 变化；支撑
[N^(6/11),N^(2/3)]；公共 mp≤N 截断。不得把有限参数表当成实数 A≥3 的证明，
不得擅自扩大到任意 m-dependent 端点、q-dependent 权或更高分布水平。

V2. 对照 Akbary–Hambrook arXiv:1309.2730v2 的引言 SW、(6.1)、Lemma6.1/(6.2)
及证明，核对任意复系数、两区间长度、q/φ(q)、最大 y、对数和绝对常数。
原 PDF 核心页截图失败并不等于公式不可读；本轮采用原作者 HTML/解析 PDF。
如取得原始 PDF 字节应另记哈希，不在候选上静默改源版本。

V3. 重新推导 (2) 的 SW→素数计数转换，含素数幂、√y 分段、d≤log^D N 到
log^(D+1)t 的转移、Li 积分起点与常数非有效性。检查 y∈[N^(1/3),N^(5/11)]。

V4. 重新推导 (5) 的主特征 −D_q/φ(q)，包括有符号权。复算 N=254,m={25,35},
q=3 的精确值。不要把提取乘积 H 与主特征移除项 D_q 混同；示意水平不是渐近起点。

V5. 检查诱导特征的 l 互素指示、d,l 允许不互素、φ(dl)≥φ(d)φ(l)，以及
∑1/φ(q)≤3(1+log Q)。固定 l 后系数是否真的对导子 d 不变？q=6,d=3,n=2 的
反例是必须保留的负面检查。

V6. 检查小导子 (9)-(10)：∑|a|/m、移除 p|l、特征个数、l 权和及 D₀ 因子。
确认不会在实际 G8 应用时再给已经吸收到 C_A 的移除项重复收费。

V7. 检查 dyadic m/conductor 覆盖和 (11)-(13)：完整矩形仍保留 mp≤N；区间长度
与上端点不能混用。核查四个根式项、5/6和8/11指数、所有 log 因子、首末截块。
数值/有限覆盖测试不替代无限范围的分解证明。

V8. 检查八项总账本、B=D=A+8,J=2A+12、D₀≤Q 的共同起点，以及对所有权的
统一 C_A。B 显式不等于 N_A 有效；SW 的非有效性必须保留。

V9. 重新验证 G8 的 f_N≤1、支撑、余类 b=N mod q、m∤N 导致的严格端点相同，
以及实际光滑筛支撑 H=0。核对筛族倍数 L_eta、归一化2、组合系数−2导致除4前成本4。
本轮没有证明 C_s 统一性，不自动闭合旧 G8 其余步骤。

V10. 实际运行 normal/-O 的 --compute 并比较输出，运行 --check 和应失败的
--require-global。核对11哈希、22负面控制、12个纯新增远端文件及原始输出。
有限大筛回归通过不能被写成一般大筛的独立证明。

对每项给 PASS / FAIL / INCONCLUSIVE，分开报告源输入、代数身份、渐近推导、
G8实例化与有限复算。记录候选/source hashes、实际命令和最早缺口。
本地 PASS 不能变成完整 Pan–Ding 审查、十二项闭合、有效 N0 或哥德巴赫证明。
修正必须产生新的 solver candidate；独立 receipt 另建 lease 和分支。
