# CP-ERR-0009 独立审查任务（未执行）

冻结对象：本 PR 最终提交中的13个文件；artifact-sha256.json绑定其余12文件，准确head/tree见PR与#99交接。不要以可变main代替冻结提交。

审查者必须使用新执行上下文及run_id，且不继承solver会话、不修补原候选。这张票据没有启动任何worker或产生receipt。修正需新候选及新的审查周期。

## 审查范围

**V1 对象与来源。** 还原 G4/G5、原截断、上界扩大、A_p不是商集、提取素数闭端点及小筛严格端点。核对最终两个系数均为-1。BJS Theorem6/Table1是导入定理；其实际质量主项、异常乘积和C1单调性不能省略。

**V2 经典BV。** 核查Vaughan p3向最大pi/Li形式的Abel转移、素数幂和端点成本，B=A0+8足够；常数独立于epsilon和p。没有使用CP6加权分布候选。不得声称有效数值N0。

**V3 预筛。** 验证固定K_dim的N一致性、w和P0先于N、产品条件对全部1<u<z成立。审查D_p=R/(P0 p)、pW_ND_p<=R、两个余量5/318和89/1166。完整目标小素数没有被删除。采用D_p>=z²是有意的保守条件。

**V4 逐素数换基。** 证明r_M,p(d)=r_p(d)-Delta_p/phi(d)及d=1的作用；严格证明(p,d)->pd单射和两区间最多重数2。证明sum|Delta_p|<=E，不能用|sumDelta_p|。复核(4+3logR)E、主项换基7E以及总(11+3logR)E；不得额外乘提取素数个数。

**V5 素数和及符号。** 检查Phi界、完整PNT/CDF端点原子、1/(p-1)修正2/z、h_star漂移、归一化尾乘积和C(N)>=1/2。重新推导(14)-(17)全部系数。确保没有重复收费，且在最终下界中符号为负。

**V6 一维化。** 独立从K2原积分推导K2'(5+w)=T(w)/(4+w)和(19)。G5分段点177/88不可删除。核查T级数的绝对收敛、A_k<1/(4k)、48阶绝对尾界和积分传播系数5/2。有限ODE身份不代替无限级数证明。

**V7 真正数值证书。** 重导J二阶导数64、K真实被积函数二阶导数1及中点余项；区分真实函数与截断多项式。运行--compute，核对全部28672点、50位区间对数、精确二进制转Fraction、18位向外舍入和两行包络。普通mp.quad不是替代证书。检查去掉K后上端点低于完整下端点。

**V8 复现/量词。** 运行--self-test、--check及-O；验证12个哈希和20项负面控制、13项纯新增diff。有限模型的X_p是合成偏移、R=N，不能当真实Li/BV数据。核查epsilon→P0→BV常数→N的顺序与delta=1e-5后果，不把固定积分cap冒充有限N实际计数cap。

## 回执要求

对每项分别返回PASS/FAIL/INCONCLUSIVE，明确候选与来源哈希、实际命令、最早缺口及受影响依赖。
固定积分、条件转移、源定理审查和全局状态分别报告；任何局部PASS均不证明全十二项闭合、原Iwaniec Cs审计、新哥德巴赫指数、二元哥德巴赫或Project admission。
