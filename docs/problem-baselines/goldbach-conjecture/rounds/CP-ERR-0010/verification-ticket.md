# CP-ERR-0010 独立审查票据（未执行）

输入为本PR冻结提交下的13文件与源锁；manifest绑定另外12文件。精确head、树和实际CI见#107/PR最终交接。
使用新上下文、run_id和independent_verifier角色；如实声明solver上下文访问。不得修改候选或自发把来源假设改成已验证。
本票据未启动任何worker或写入独立receipt。修正需新候选/lease。

## 必查范围

V1. 从Li–Liu v2还原原始G6、严格输出截断、提取素数闭端点、未除以m的A_m、归一化53与最终系数+1。真实平方对角和离散主项对角不是一项。

V2. 审查BJS v6 Theorem6/Table1及表后单调性，常密度特例实际质量M、例外乘积W_N、D>=z²的适用条件。154是导入常数，不把本轮测试称为原表重现。审查经典最大BV转π/Li的对数选择B=A0+8、d=1及严格左极限。

V3. 证明维数一K_dim可以在N/小参数前固定；核对w、P0、W_N以及全支撑d<W_ND_m。固定模式D_m=N^(1/2-sigma)/m需h_star<=sigma；对数模式D_m=R/(P0m)需另一几何条件。sigma=0不能冒充可用固定模式。

V4. 独立推导(p1,p2,d)->p1p2d单射和sum_m|Delta_m|<=E，核对p1<p2、平方自由、互素、d=1包含。不能把|sum Delta|当作sum|Delta|，也不能将平方对角的phi当作平方。

V5. 核对实际质量换基与倒数phi和界。检查|f-kappa|<=6、kappa<=77epsilon及负主因子情形，重新推出(10+3logR)E与归一化23 C_BV T^(3-A0)。全部额外成本都要有正确符号。

V6. 核对C(N)>=1/2、32/21截断、q_N/r_N一致收敛、CDF下端点原子、两次Stieltjes、坏素数质量与半个离散对角。检查正项用1/phi>=1/m、负项独立上界。I-D<0时仍须有效。

V7. 重导(3)/(21)全部系数64/21、2、2809/44、2915/4、8162、23及对数模式2809/8；固定sigma成本必须已经包含在g_sigma中。不得同时使用g0和零漂移。

V8. 由原f公式重导psi、T及C2拼接；证明Taylor绝对尾界和M2=102576253/256。移动积分断点必须为21/106-sigma。执行--compute重算全部36864中点；检查50位有向算术、二进制元组到Fraction、18位向外舍入与解析余项，不仅检查保存结果。

V9. 执行--check、-O --self-test，确认12哈希和20负面控制；--require-global必须退出1。有限数据采用合成X=3N/7和模型筛界，不能解释为观察到的Li误差。检查13项纯新增diff、父候选不变、相对链接和冻结字节。

V10. 审查sigma=1e-6的取舍和4e-6的失效解释，delta=2e-6的局部最终下界，以及参数选择顺序。几何起点模板不是完整有效数值N0；导入定理和候选缺少独立receipt时，不能升级全局结论。

## 要求输出

逐项PASS/FAIL/INCONCLUSIVE，分开报告固定积分、条件解析转移、源适用性和全局范围；给出最早错误/缺失假设、精确输入哈希及实际命令。
局部PASS不推出完整Li–Liu定理、二元哥德巴赫、有效数值N0、原Cs审计完成或Project authority。
