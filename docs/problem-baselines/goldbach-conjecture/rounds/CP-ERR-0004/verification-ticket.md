# CP-ERR-0004 独立审查任务（尚未执行）

候选：本 PR 的冻结 head 下 `docs/problem-baselines/goldbach-conjecture/rounds/CP-ERR-0004/`，12文件；`artifact-sha256.json` 绑定其余11文件。
准确提交号与远端树见 #70 / PR 最终交接。不得用可变 main 代替冻结 head。

必须使用新执行上下文、新 run_id、independent_verifier 角色，并如实声明是否接触 solver 上下文。本票据没有启动 worker、授予新 lease 或出具 receipt；不要修改候选来“验证”它。修正需要新候选。

## 逐项审查

V1. 对照 arXiv:2606.05224v2 还原 G6、原始严格截断、两个素数闭合端点、S_N、psi 和系数53。确认组合系数+1，最后除4。

V2. 检查真实对角的下界删除、与离散主项对角的不同作用。证明 (p1,p2,d)→p1p2d 单射，检查 d<z-smooth、p1<p2、平方自由、d<Q_m、(N,p1p2d)=1 及严格端点 BV。不同 m 的权重不能无依据合并为同一函数。

V3. 重新检查 U-LS 的量词：Cs 是否真的只依赖固定 K、独立于 eta/epsilon0/N/提取素数；原文符号“<<”本身不足以代替文献审查。U-BV/U-PNT 为输入，非本程序证明。取得确切版本 PDF 后另记字节哈希，不能把网页渲染哈希冒充 PDF 哈希。

V4. 核查水平下界901/264、上界37/8，几何余量373/3498及采用余量109/3498；logQ≥3a logN 和 e_star 的归一化。不能给 fixed/log 模式删除 h 误差。

V5. 证明 psi、psi'、psi'' 的紧区间界、s=4 的 C2 拼接；核对截断32/21、C(N)≥1/2、有限奇素数乘积恒等式与大素因子尾项。r_N 必须含下端点原子。

V6. 核对 Stieltjes 两次转移、半个正方形、去公因子质量≤1/(az)、离散对角M/(2z)与 hM²/(2a)。下界正主项里1/phi≥1/(p1p2)有利，但负罚项必须单独上界；检查 I−Delta<0 情况。重新推出七个系数64/21、2、2809/44、2915/4、2809/8、212、2。

V7. 从二维三角积分独立推导 W(t)，确认无遗漏2或1/2，a+b与21/106不是同一断点。推导 T 的系数公式、A_k<1/(4k)、48阶绝对尾界及其积分因子53/6；不要只相信有限递推测试。

V8. 检查 M2=102576253/256 及每段复合中点误差。运行 `python check_g6.py --compute` 重算全部12288个点；核对50位区间运算、内部二进制元组转精确Fraction、18位向外舍入。`--check` 不是求积重跑。log-only 上界不足仅否定这个简化比较。

V9. 运行 `--check`、`-O --self-test` 与必须失败的 `--require-global`；检查11哈希、14负面控制、最终远端12项纯新增 diff。有限测试、数值证书、条件证明及来源验证须分别报告。程序/证明用同一上下文写成，不能称独立。

V10. 审核小参数选择先于 N 的逻辑，delta=3e-6 的局部后果与未提供数值 N0 的边界。未协调其他十一项及全局源门槛前，不得把本轮增益插入全局账本或升级论文/哥德巴赫状态。

## 输出

对每项给 PASS / FAIL / INCONCLUSIVE，分开报告固定积分、条件转移、源假设和全局状态；写明最早错误或缺失假设、候选/来源哈希、实际命令及 cannot_imply。
即使局部 PASS，也不证明完整 Li–Liu 定理、新哥德巴赫指数、二元哥德巴赫或 Project admission。独立 receipt 的写入需另建 work packet、lease 和 branch。
