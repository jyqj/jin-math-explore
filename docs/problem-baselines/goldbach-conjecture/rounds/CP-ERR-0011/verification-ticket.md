# CP-ERR-0011 独立审查票据（未执行）

审查本 PR 冻结 head 的13文件；manifest绑定12个有效载荷，准确head在PR最终交接。
须用新上下文、新run_id，声明未读取solver对话；不得修改候选或自行把当前作者变成独立verifier。
本票据没有启动worker或签发receipt。

V1. 从S1原定义恢复G7、原始严格截断、系数53与最终系数+1。核对遗漏gamma cap和额外1/v为继承诊断。
重新证明N^(4/33)不可能是素数，确认矩形既不除2也不引入三角对角罚项；不要推广到任意实阈值。

V2. 核对BJS定理的实际质量、常密度、C2<=154与W_N D_m余数支撑。
证明预筛乘积条件、完整P0水平、两个模式及u+v<=c-delta，先删非负项再调用下筛。
检查s=2、空序列和负主因子情况。BJS原表不是本程序重算的。

V3. 重导严格AP恒等式、r_M换基、两大素数单射（含d=1）、绝对质量之和及倒数phi对数成本。
重新推出(7+3logR)E和17 C_BV T^(3-A0)。核对Vaughan至pi/Li的端点、对数选择与ineffectivity。

V4. 重导连续零延拓核及其[1/200,1/2]导数界；monotone-CDF分部积分须含起点原子。
核对两次测度转换r_N、删p|N质量、11925/16系数，正分母比较与负罚项须分开。
检查I-deficit<0时归一化不等式，独立重算所有六项系数。

V5. 区分I_delta、完整g_delta和实际G7。证明总损失线性上下界和strip二次界，不能重复计费或将总损失当二阶。
重导第五阶小角界，核查s>4支持、uv>=ab、Jacobian与积分e^5/20。

V6. 从矩形切片重导W与三个分段，核查gamma cap。重算H的导数、333783040000/185193和各段中点误差。
运行--compute确认全73,728点；检查50位区间、精确二进制端点及18位向外序列化。上端点只约束固定核。
特别复核sigma5e-7的失败已包含正修正上界，不把它当G7上界。

V7. 运行--check、-O --self-test及必须失败的--require-global，检查13文件与12哈希。
有限AP采用合成X，monotone测试采用替代核，不能将2327实例称为源定理证明。
normal/-O只复算有限/检查模式，本轮主求积只有一次。

V8. 核查共同小参数选择及h_star<=sigma全部代价；未给有效N0、未计算P0、未更新全局账本。
分别报告source applicability、局部推导、计算复现与全局边界，逐项PASS/FAIL/INCONCLUSIVE。
即使局部PASS，也不签发哥德巴赫证明、其他十一项结论或Project admission。
