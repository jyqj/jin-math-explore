# CP-ERR-0005 独立审查票据：尚未执行

读取本PR最终冻结提交下的12个文件；提交号和树哈希见#74最终交接。`artifact-sha256.json`绑定其余11文件。
使用新上下文、新run_id、independent_verifier角色；不继承solver对话，不修改候选，不代替源门槛填入PASS。本票据不启动worker、不授予lease，也不是receipt。

## 审查范围

V1. 对照arXiv:2606.05224v2印刷页38的G8，检查A_m不是除去m后的序列、P(Np1)排除p1、闭合提取端点及严格输出截断。证明原生Q_m<=N^(-1/22)/log^B N，限制“不成立”只指这条直接路线。

V2. 核查N254的反例；重证任意p>=37的Bertrand构造、所有量词、gcd、p1^11>=N^3、pq²<N和epsilon0截断。它反驳的是源端精确等式，不是论文主定理。T<=G8的方向及每输出至多3个提取对也须审查。

V3. 重证平方、残余1、输出素数整除N的三个异常界，确认不存在漏掉的复合残余。检验0<=G8−T<=E0及S_B+3Z扩大关系。上界不能免费删除正异常支路。

V4. 核查多重序列与M_N的不同重数：f_N(m)<=1不表示(m,p)->b单射。验证N4032重复三次和N254复合b例子。不能去重后仍用原X/AP余数。

V5. 核查Z=sqrt(Q)、s=2、F(2)=exp(gamma)，以及Z<N^(3/11)。在q|P_N(Z)上证明(m,q)=1和H=0；检查q=5,m=35的离开支撑反例，禁止把H=0推广到所有q<Q。

V6. 独立检查加权U-WBV原始来源能否覆盖N-dependent f_N、支撑N^(2/3)、可变余类N和严格端点。未读到原文就保持源门槛，不把S1的引用当作原始验证。检查U-LS的Cs统一性及有限L；当前候选明确依赖它们。

V7. 验证两次Stieltjes转移及CDF左极限：固定u的6r_N界、外层F递减及F(c)<1、闭端点对角已包含、总7r_N。审查Li缺陷区间和去公因子条件的上界方向。

V8. 重导C(N)>=1/2、移动Z的N-uniform产品尾项、x_N<=1、主系数8/(1−2h)、所有七项误差系数及组合−2。核查L只乘衰减余数，不乘固定eta；无不当删除截断下端点，因为B和X已整体扩大。

V9. 独立推导c_n递推、绝对尾界、因子8及换元区间；实际运行`python check_g8.py --compute`，比对完整有理数，而非只看18位小数。检查upper cap是积分上界，不是G8计数的两侧区间。

V10. 运行`--check`、`-O --self-test`和必须退出1的`--require-global`，核对11哈希/16负面控制、593个N的确切有限范围、1141存活项/776有序三素数项及268个等式不符N。有限样本不替代无限族证明。验明远端diff只有本目录12项新增。

## 输出要求

按局部反例/组合修复、积分证书、条件上界、源输入及全局状态分别给PASS/FAIL/INCONCLUSIVE；写明新actor/run、候选和依赖哈希、实际命令、最早缺口、影响范围与cannot_imply。
局部PASS不能证明哥德巴赫、改进全局指数、闭合十二项、提供有效数值N0或升级Project。修复只能作为新solver候选和新审查轮次。任何receipt写入另建工作包、lease和branch。
