# CP-ERR-0007：G8 的显式线性筛替代接口

关联 #87，承接 #81 / PR #84。**reference-only solver candidate，未独立验证。**
旧候选、数值主系数及全局账本均不修改。

本轮没有把无法访问的 Iwaniec 原文伪装成已审完，而是把问题分开：
原 C_s 统一性审计仍未解决；G8 支路可否不再依赖该未知常数？

[完整推导](proof.md) 给出一条替代候选：使用 BJS v6 Theorem6 的显式常数，
先固定小素数集合并将水平降到 D=R/P0，再把实际序列质量与原 X 精确换算。
由此得到 G8 的六项误差契约，**不再出现 C_s 或 L_eta**；代价是一个固定素数乘积的水平成本及一个额外对数损失。
原加权分布候选仍是待独立审查的依赖，不能据此宣布全局闭合。

另发现原文严格支撑 q<Q 与允许因子水平1不能同时给出非零 well-factorable 权。
这是明确的定义端点问题，可用闭支撑约定修正；不构成哥德巴赫反例或论文主定理反驳。

实际重新计算固定积分，仍得
`g8 ∈ [0.609611573201938690, 0.609611573201938694]`，没有声称刷新主系数纪录。
在声明输入下，本轮可重新保住原显示上界0.60962，但未给出有效数值 N0。

## 复算

Python>=3.10，无第三方库。在本目录运行：

```sh
python check_sieve.py --compute
python -O check_sieve.py --compute
python check_sieve.py --check
python check_sieve.py --require-global  # 必须退出1
```

前两个命令真正重算并比较冻结输出；`--check` 只检查11个哈希及18个负面控制。
有限回归共4287项计数，包含805个重定基准恒等式、748个预筛模数范围检查、
243个主项放大不等式，以及实际 G8 多重序列测试。它们不证明源定理或无限量词结论。

[机读契约](contracts.json) · [既有积累](prior-work.json) · [来源与访问边界](source-lock.json) ·
[误差/下一步](error-handoff.json) · [计算记录](computation-handoff.json) · [独立审查票据](verification-ticket.md)。

所有12个新文件纳入本轮 PR，不以一个暂存分支代替交付。全文源文件字节哈希未取得，
本地全仓测试未执行；远端 CI 仅按 PR 实际结果另记。下一步是 G1/G2 下界的同类替换，
并保留依赖 well-factorable 加权平均的其他支路边界。
