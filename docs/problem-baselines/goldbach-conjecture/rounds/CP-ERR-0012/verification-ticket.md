# CP-ERR-0012 独立审查票据（尚未执行）

候选根：本PR冻结head下 `docs/problem-baselines/goldbach-conjecture/rounds/CP-ERR-0012/`。准确head、tree和远端CI见#119/PR最终交接；不能用可变main代替。清单绑定另外12个文件。

使用新上下文、新run_id和 independent_verifier 角色，只读冻结候选及来源。继承本次solver上下文者不得称独立；本票据没有启动worker或授予写入lease。修复属于新候选，不修改本冻结包。

V1. 对照S1的原值A_m定义和G3/G10对象：p2本身是否会在p1*p2^3<N时被筛去？重导活跃域、2/11断点、[41/99,7/11]支撑及共享端点不可能性。不得把商序列的结论混入。

V2. 检查142−7=3^3*5及第3.1节的全部指数、互素、截断和筛条件；证明反例族无界。分别给局部残余推断与整篇定理的范围，不凭局部失败宣布全局反驳。

V3. 重导4k+9l<33的重数上界6和G3重数2，三种异常预算、小输出成本及原计数到带索引B_j的注入。检查示例370−139=21*11=33*7不能去重。正异常必须扣预算，不能免费删。

V4. 对新slab分布命题逐项审查：N-dependent但q-independent复权、公共ms<N、最大约化余类、主特征D_q、导子q=dl的互素过滤、SW一致性、全部dyadic块和对数因子。重导八项账本和B=D=A+8、J=2A+12；不能用CP6较窄命题代替。核对S3 Lemma6.1的原始范围，承认SW非有效性。

V5. 证明真实G10光滑支撑可能有H>0，区分提取m的非互素与残余素数s|q。核对N10002,m65,q5及无界族；重导H总界6N(1+T)^2/(N^beta−1)，注意q平方自由和只有p1可能小于Z。H(1)=0不能误写H(q)=0。

V6. 检查BJS全部输入、表常数154、实际质量而非任意X、P0和W_N、d<W_ND<=R。逐式核对r_M=r−H−Delta/phi及归一化19C_A T^(3−A)，不能再对H加一次换基对数或重复收费主特征项。

V7. 重新推导闭端点Stieltjes公式和G10外层J的单峰性；核查内误差9r、外3r，共12r，原始tau差成本33epsilon_tau，乘积展开的全部常数。独立推出两项合同和联合式(30)的十个系数，保留−4和−1。

V8. 复核一维积分化、解析左段8log2log(29/18)、21000二阶导数界和每段误差。运行 `python check_switching.py --compute` 评估8192个中点并核对完整输出；检查32项有理对数尾界及向外序列化。旧、活跃、删除区域三个证书应分别有正确误差，不能重复用总误差充当精确差。

V9. 运行`--check`、`-O --self-test`和必须退出1的`--require-global`。5819个计数实例不是独立数学结果，AP质量为人为X_m，CDF为有理替代模型；只可称有限回归。核对13新增文件、12哈希和20负面控制，CI不是数学验证。

V10. 核查参数选择、余量意义及因果依赖。只有完整源输入和其余十项/组合式审查通过，才能讨论全局数字。原Cs审计、Pan–Ding全文、原BJS表重建、有效数值N0均未完成；不得把“本轮不需要”写成“已审计通过”。

逐项输出PASS/FAIL/INCONCLUSIVE，分别报告对象/有限身份、分布桥、筛转移、固定数值、源接受与全局状态。写明最早缺口、实际命令、冻结候选/源身份和cannot_imply。即使局部PASS也不自动证明二元哥德巴赫或完整Li–Liu定理。receipt写入需另开work packet、lease与branch。
