# 隔离验证任务：C12-01..06

这是待领取的任务，不是独立receipt。使用新run_id和上下文，只接收本目录12个冻结文件、manifest、#113和锁定来源；不得继承solver对话或修补候选后宣称原候选通过。精确commit/tree由最终PR handoff绑定。

V1：检查联合允许集的平方自由、辅助互素、W及ab<=Z条件；(1,1)系数确为1，端点素数大于Z。证明beta的D/phi(D)界；不要把两个一维筛的乘积与本轮一个联合平方混淆。

V2：独立证明G2/Bx²->z²/2。核查变化W的一维源引理适用性、t=0、固定矩形夹逼及边界；互素删除必须有明确O(Bx²/w0)上界，不可引用“显然独立”。有限180例只是代码回归。

V3：重建117个状态、14类及全部混合矩；证明delta=2092q²+13984q³+2736q⁴<=5759q²。辅助–保留共享不可删除。核查张量展开对任意有符号、非分离、x依赖数组成立；分清D是比较模型，K才是真实CRT。

V4：重新核对B、G2、W幂次和mathcal C_x。证明两份系数总变差、严格2gamma+2z<1和固定M统一相加o(1)；不只验证模数幂次。核查常数恰好为2rho_*²/z²，支持边界不可取。最优性仅是固定允许集的对角能量；不能写成所有筛法最优。

V5：核对有限输入与T11公开定义逐字一致，独立重算周期/区间总和。新均值不可替换为独立均值，新小区间上界恰好改善不说明所有区间改善。核查相同27纤维的两组凸优化证书以及16个零容量纤维；整体目标不宣称减半。

V6：审查物理恢复桥：(H_O)_Y测度<=剩余总半径²/2来自总质量密度<=1；不是把nu当概率。P_O是H_O内乘1_O，故差e仍支撑于H_O。核查加权迹算子、平方根交叉项、theta先固定、实际q/a/M_det缺失，以及全部范数同尺度。该Hilbert空间桥不自动提供助手实现或单素数估计。

实际复现：新目录生成并逐字节比较inputs/certificates/results；运行正常/-O self-test和hash check；--require-global必须exit1。checker导入generator只是冻结重放，独立路径是字面剩余类、整数容量、周期/区间计数和辅助函数重建。不要把这些同context路径当成隔离数学审查。

输出按C12-01..06分别PASS/FAIL/INCONCLUSIVE，绑定候选/源哈希、真实命令、最早错误与cannot_imply。修复需要新solver候选。下一solver可先认证未恢复双迹成本q_plus、删除范数a_plus及合法助手容量/实现误差，再比较同尺度检测余量；不要重复已完成的矩形调参或将候选误升为184/孪生素数结论。
