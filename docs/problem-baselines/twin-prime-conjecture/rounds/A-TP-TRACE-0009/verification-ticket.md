# 隔离审查任务 T9-01–T9-05

此文件是任务，不是receipt。新上下文、新run_id、independent_verifier角色，仅接收12文件冻结包、源定位、本任务与治理；不得继承solver对话、修改候选或把两套代码当作独立人次。最终commit、subtree及manifest自身哈希在PR/Issue handoff绑定。

V1. 检查有限系数迹相等是否覆盖全部内部变量；严格R<素数端点；平方非负方向；两个有限反例（改变迹、允许端点本身作为因子）。实际有限fixture必须非零，12<=342并非生产相关性估计。

V2. 独立推导A、K、J_a-K和逆权分母。核查零宽纤维/积分可交换性；CS只给能量下界，恢复序列才能产生可用的算术上界。

V3. 检查光滑恢复G、正交体原函数H及chi延拓，整个双边界相等、严格径向余量、导数符号、J_a=kappa*K；固定epsilon之后取N极限。若恢复证明有缺口，应返回缺口，不修补原文件同时宣称PASS。

V4. 核查条件算术契约的同一lambda参数化、Gamma、W、R和所有被调用单素数估计。Maynard Proposition4.1的lambda不是Chen的mu*f，不得直接互换。分开判定一般接口与实际186试验权；后者未计算、未证明契约适用。

V5. 推导5种矩阵条目的阶乘、内部坐标数38/37以及统一40!因子。对k=4,5,6独立符号积分；确认子空间包括端点不对称，却排除了内部非径向结构。证明Bernstein基独立和I严格正定。

V6. 验证双边界迹的d+1个独立分量、所有核基列和R'KR=0；d6为84、7、77。检查所谓零惩罚只指该边界形式，不是全部筛损失为零。

V7. 逐个重建20个目标与602个有理正主元、全部下界整数向量；上下界宽度<=1e-6。正常/-O与hash-check，--require-global必须exit1。冻结向量的精确验证不需要NumPy/SciPy，浮点只用于可选提案。独立程序路径不等于上下文隔离。

V8. 核查一阶反模型41个状态、等均值但非完整联合交换性；它不是素数模型。核查失败解释：普通J/I已经<4，不能把失败全归咎于端点扣项。

给每个T9 claim返回PASS/FAIL/INCONCLUSIVE与精确依赖哈希、实际命令和最早缺口。任何PASS不得外推为真实186输入验证、DHL39/38、184新界或孪生素数证明。下一solver任务是实际扩大支持的双边缘/同尺度分布桥，不是重复这一低阶普通单纯形调参。
