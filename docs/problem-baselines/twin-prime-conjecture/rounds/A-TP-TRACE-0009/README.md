# A-TP-TRACE-0009：端点相关性从待估计项到可计算形式

继续冻结PR #88的端点排除路线，工作包#94。本轮完整12文件归档只新增本目录；#88、#80和其他研究文件不变。数学等级为solver候选与有限精确证书，尚无隔离审查。

**来源进展。** 找到Bin Chen arXiv:2402.00748v2介绍的Tao边界保持法，它已包含同型端点扣除机制。本轮不再把该检测原理当作新颖性主张；详见proof.md §0。

**推导进展。** 将双素数项上界接口化为双端点迹A的能量K=integral(A²/L)。给出投影恒等式、加权逆长度版本和保持整个边界的光滑恢复序列，明确CS下界不能直接偷换为相关性上界。其传入算术的单素数归一化与支撑条件单独列明。

**实际计算。** 在40维普通单纯形上，对两个端点及38个内部坐标之和构成的次数<=6多项式族，导出精确矩阵并完成0–6次共20个变分证书。最高族有84参数；惩罚矩阵秩7、零迹子空间77维。

| d=6的全系数极值 | 严格有理证书范围 |
|---|---|
|普通一阶商J/I|[3.164150,3.164151)|
|扣端点商(J-K)/I|[3.143126,3.143127)|
|77维零迹子空间J/I|[3.086033,3.086034)|

**结论是受限失败，不是新间隔界。** 即使宽松取经典sigma=1/4，扣端点评分上界仅0.78578175<1；这个族连未扣项的一阶检测也未通过。排除的是该84维族的全部实系数，不是所有40变量函数、扩大支持或其他筛法。实际186试验权尚未映射到这些形式，旧余量不允许直接搬过来。

## 复现

仅验证冻结证书需要Python标准库：

```sh
cd docs/problem-baselines/twin-prime-conjecture/rounds/A-TP-TRACE-0009
python -B check_trace.py --self-test --check-hashes
python -O -B check_trace.py --self-test --check-hashes
python -B check_trace.py --require-global   # 按设计exit1
```

可选重新提出向量（NumPy/SciPy仅为提案工具，所有接纳仍使用有理运算）：

```sh
python -B trace_forms.py --propose /tmp/twin-trace-fresh
```

不同浮点库可能提出不同整数向量；不要求跨后端的向量字节相同，必须通过完整精确checker。certificates.json包含全部向量和上界，不需要外部bulk数据。测试覆盖602个正主元、2005项独立代码路径的符号积分比较、15项破坏拒绝、27个加权纤维以及非零有限迹回归。两套代码不是隔离verifier。

下一接口：真实186权的离散边界、合法双边缘支撑、相应单素数估计与原余量的同尺度误差账本。verification-ticket.md只排队待审，不代表后台worker。主分支基点39002c5a6af8c7b7f093589e6a76cfd218fcbb99；分支program/i-0094/twin-endpoint-trace-09；不请求合并。
