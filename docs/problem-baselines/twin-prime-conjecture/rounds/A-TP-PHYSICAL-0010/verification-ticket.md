# T10-01..05 隔离审查任务

这是一份待领取ticket，不是receipt。输入：本目录12个冻结文件与manifest、#100、最终PR中记录的commit/tree、source-lock里的原始定义。不继承solver对话；须使用新的run_id和上下文；不在候选上边修边声称通过。

1. 核对77系数、全部签名、中点偏移、cap和radial边界与固定Challenge.lean。审查两次对角边缘的归一化、素因子截止、平方自由及跨根互素约束，重证逆变换。物理函数的空坐标值绝不是此处的对角边缘。
2. 审查纤维容量的充要条件，零容量与倒数不可积情况、最小范数的唯一性；检查实际cap阶梯非增、端点等号及不额外乘cap/zeta。光滑性、渐近与modulus admissibility不由这个Hilbert空间计算提供。
3. 核对38个index的和98263、全40中点和98283h、唯一端点cell对(0,0)。复核截断物理测度在各cell的质量是h、盒测度h^38、误差下界h^42 V^2。不得对任意有限测度错误使用“支撑位置<=总质量”；本文盒有显式cap。检查全部有符号项而不以绝对系数代替V。
4. 复核V的两种独立代码路径，完整有理数及单侧区间；区分正测度盒证据与单点证据。明确它只排除未经恢复H的精确内层匹配，不排除近似匹配，不验证P_OH，更不反驳186或孪生素数。
5. 重证A_y=B_z+D_u在端点双素数事件上的身份、带tau的残差项、两端上筛平方的方向。CRT必须保留有符号系数和非互素兼容性；正确的floor误差不能与主项混同。
6. 审查G_Z(r)的所有互素约束、B^2提升与B^(4-k)范数。重证其最小性。检查根支持指数0.9485994；不得把这一条件当作sup范数/总变差/容量倒数或渐近主项已受控。
7. 新目录复现发生器，逐字节比较证书/结果；正常与-O执行self-test和hash-check；require-global必须exit1。152对角等式、投影、CRT与有限素数例均须区分于一般证明。检查破坏测试确实经真实validator拒绝。

逐claim返回PASS/FAIL/INCONCLUSIVE、精确输入哈希、实际命令及最早错误。若缺来源或恢复对象，不能补写为已验证。修复形成新的solver候选。即便全部有限检查通过，也不授权宣布新的prime-gap界、实际残差足够小、独立输入全部验证或Project完成。
