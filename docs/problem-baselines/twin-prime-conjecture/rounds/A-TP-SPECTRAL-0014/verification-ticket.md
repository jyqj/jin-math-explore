# 隔离审查任务：R14-01..05及环带归档身份

使用新的run_id和上下文，只读取冻结候选、manifest、明确来源与此任务。
本solver已看过历轮对话，不能签发独立receipt。修复必须成为新的solver候选。

1. 审查任意有限非概率乘积空间的P_i：归一化1/T、自伴幂等、相互交换、有限Q_A正交分解。
   核对J/T与2*sum K/T²，尤其s是常数坐标数。证明整数因式(s-ell)(s-ell-1)>=0；
   明确无对称性时仅得到平均对，不可升级为指定端点对。
2. 检查所有c<=T及kappa>=rho/T时的权下界；核对固定耦合和自适应表达式的最小kappa。
   当前输入下的有理log10截断、Euler常数界、zeta区间与8rho*zeta>1均须重算。
3. 对照固定Challenge.lean，检查H的置换对称性、J_all>=J_lambda及统一Z。
   数值括界是条件输入，不是此次验证得到的实际积分。核算L、U、比值和阈值。
   最关键：U不是未知真实余量的上界；不得把此候选标为整个方法不可能或论文反驳。
4. 审查有支撑去迹投影、零容量、有限能量、L-infinity界和Pythagoras最优性。
   检查(14)只适用于满足原括界的未恢复H，检查源总质量密度及三角形面积输入。
   去迹和再次删除不交换；新试验尚无正性或算术实现。全对称零迹的J<=T I应单独证明。
5. 在新目录重生成并比较字节；正常/-O运行完整检查及hash check，全局请求预期exit1。
   直接边缘与谱路径是机械交叉检验，不代替上述证明。源字节哈希未知处不得补造。
6. ANNULUS-0013完整上传仍被工具拦截，不在本PR变更中；本地重放与输入ZIP的12文件身份已记录。
   历史no-PR文字不得重写为旧运行成功。未来允许回收时另核对逐字节身份。
   区分PR121的耦合12、PR126的自适应13与本地环带13。CI通过不改变这些数学证据边界。

逐项返回PASS/FAIL/INCONCLUSIVE，绑定候选/依赖精确哈希、实际命令、最早缺口和不能推出的结论。
一个有范围的PASS仍不验证原数值积分、不推出184或孪生素数，不关闭Project数学目标。
