# 隔离审查：A13-01..04

本文件是待领取任务，不是receipt。使用新run_id与上下文，只接收本目录冻结12文件、来源与#122；不得继承solver对话、修补候选后声称原候选通过。精确head/tree见最终PR handoff。

V1：重推根依赖L_r、端点事件V=D及G_r的完整归一化。检查辅助根与保留根可共享；核查两不同根的乘子1/8和23/142排除共同乘子。不能逐根平方、删交叉项或把扩大的原始对角数组当成全局平方自由。

V2：检查变化W的一维源输入与二维互素删除；必须由单调性和固定网格建立[ell-gamma,ell]上的一致极限，才能一致取倒数。ell-gamma>0的固定正距离不能省略。

V3：重建局部K/D及非分离张量误差、全局sup界、精确比较均值、B/G/W幂次和全系数总变差。对根依赖系数不得引用不再存在的乘积范数分解。2ell<1必须有固定余量。区间CRT兼容条件与原始共享根是不同层面的限制。

V4：检查变罚系数下全部助手的配方及theta凸性；实际助手单素数评价、有限带逼近、weighted离散–物理收敛仍是外部义务。检查t=rho*s、gamma=rho*S和总质量密度<=1。

V5：重新求(S-s)/(ell-rho*s)的单调性；确认gamma²/(theta ell²)只是合法粗上界。新旧比例20.31%不代表真实最优算子范数或恢复误差节省。恢复传递式保留交叉项且q、a、余量同用40维正归一化。

V6：新目录重放发生器、正常/-O self-test及哈希；复核27个根的全部容量、665个原始状态、540个共享状态、442225个新CRT有序对、完整385周期及1000个区间整数。直接反演与U展开不只比较旧日志。n1043处的反例限定于有限平方单调性；126个端点素数对上的等式仍须通过。

V7：重新审核两个有限助手模型的凸括区间、16个零容量纤维和25个篡改/非法参数拒绝。两个最优值不是C12使用其他kappa的数值，也不是真实186物理目标。--require-global须exit1。保留初始负面检查错误和纠正原因，不把测试通过当成全数学正确。

逐项PASS/FAIL/INCONCLUSIVE必须绑定候选和来源hash，记录实际命令、最早不满足条件及cannot_imply。任何修复形成新solver候选。本候选不涵盖真实q/a值、检测正性、源论文独立验证、184界、孪生素数或Project完成。
