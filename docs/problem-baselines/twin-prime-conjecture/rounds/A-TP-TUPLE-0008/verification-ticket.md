# 隔离审查任务：T8-01 至 T8-05

本文件是待领取任务，不是receipt。输入为本目录12个冻结文件、manifest、#85及其v1.1补充和其中锁定的源定义。冻结commit/tree以最终PR及Issue handoff为准。须使用新run_id和上下文，不继承solver对话；不可在候选上边修边宣称通过。当前两份代码的交叉检验不满足这一隔离条件。

1. 复核整数平移、模2、除2对奇素数的双射、p>k基数桥梁与偶数直径排除。不得把有限检查直接外推到原猜想。
2. 复核缺失类递推的双向覆盖，尤其r=0为何禁用、>=k剪枝为何安全、重复状态为何可合并。正例必须全为恰好k元素；一般情况下大状态需要展开所有k子集。
3. 新目录重跑位集生成器；独立重建每层集合或其他精确方法。比较六实例、完整终层、58个正例与反射闭包，不仅比较摘要。确认k40D184的六个中间状态确实各自覆盖全部模19类；对H38/H39亦核查前一偶数直径不可行。
4. 核对上游固定Challenge.lean末尾admissibleTuple与inputs.json，不导入三个外部公理、DHL结论或152个积分不等式。确认tuple本身是26个最优40元组之一。
5. 运行正常和-O self-test、hash check、--require-global（预期exit1）；确认75个字面枚举测试没有先行假定奇偶归约，20个破坏fixture是真正失败。哈希匹配只说明输入相同，不等于数学正确。
6. 审查D=m+a+b-1-ab所有156状态及有限非负权的正和传递；检查区间随N无界是得到无穷多素数对的必要范围条件。确认唯一距离186的点对是端点对。所有其他偶数距离至多184，选其间的相邻素数对不会增大距离。
7. 核查cannot_imply：H40不是真实prime-gap liminf下界；DHL39/38未证明；原线性检测余量没有自动覆盖端点联合素性项。若称新颖，必须另做足够文献核查；本候选未声称新纪录。

输出应逐条PASS/FAIL/INCONCLUSIVE，绑定精确文件哈希、实际命令、最早错误或缺口。T8-03可给有限枚举审查，T8-01/02/04/05必须另外做数学语义审查。修复需要新solver候选；不得颁发超出这五项范围的PrimeGaps186、孪生素数或Project完成声明。

后续solver可选明确目标：为固定40元组构造同一权下端点联合素性上界，严格小于线性检测余量；或者建立适配39元组的新解析证书。两条路线均尚未执行，也未启动后台worker。
