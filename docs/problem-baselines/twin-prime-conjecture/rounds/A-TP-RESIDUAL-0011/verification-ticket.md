# R11-01..05：隔离数学审查任务

这是冻结输入清单和待审任务，不是已执行receipt。最终commit/tree见PR及#108交接；manifest绑定11个payload。须使用新上下文/run_id，不继承solver对话，不修改候选。出现漏洞返回FAIL/INCONCLUSIVE，修补归新solver候选。

## 重点审查

1. 局部状态必须为两个任意辅助标签加至多一个保留标签，共4(m+1)。核(3)必须包括三重共享和六阶混合矩。复核26类计数、一般m误差多项式、其绝对值符号条件以及m38的33292上界。有限74,752项通过不是所有m/p的证明。
2. 张量逐元素绝对和界须适用任意有符号、非分离、x依赖系数向量。逐项检查B^{-m}/(G_aG_b)、prod d_p的k=m+2阶增长、误差B^{-k}/w0和主项B²/(G_aG_b)，不得漏一个辅助因子。
3. 对照Maynard Lemma6.1的全部参数验证(8)对变化W统一；校对Mertens乘积和相加o(1)。区间错误须由总变差界导出，不能只凭模数最大值。W必须含全部偏移差素因子；不兼容CRT项必须为0。
4. 区分T10最小对角提升与本轮乘积筛路线：本轮允许跨辅助/保留根共享素数，只在碰撞误差里处理，不能宣称G_Z(r)=G_aG_b。复核残差上界是否确实不需要素数分布假设，而助手项明确需要。
5. 对全部助手系数复核(13)–(14)的配方恒等式、零容量分支及sup/支持界。审查theta凸化、无界tau边界、导数有理括区间和切线下界。助手有限模型的0.111722941029..不能当作实际186计算或有限区间上界。
6. 独立复现新目录生成、正常/-O self-test、11个哈希和全局拒绝exit1。直接核对385个周期位置、1000个区间整数、126个端点素数对、23个破坏/非法参数测试。确认所有输出可仅由本目录字节重现。
7. 对每个R11 claim分别PASS/FAIL/INCONCLUSIVE，并绑定输入hash、实际命令和最早缺口。没有实际恢复数组的同尺度正性证据时，禁止给新prime-gap、孪生素数或Project完成结论。

## 后续solver的单一可验收目标

锁定真正的恢复轮廓P_OH或对角数组y，以及一个已满足self-pair模数分布条件的助手支持E；评价A=T_bT_a y在容量c上的带权分布，选固定theta，计算(15)并包含所有恢复/逼近损失，再与同一C_x正规化的检测余量比较。优先产出这个量的上下界或精确失败边界，不重复已封存的普通单纯形次数6优化。未启动任何后台worker。
