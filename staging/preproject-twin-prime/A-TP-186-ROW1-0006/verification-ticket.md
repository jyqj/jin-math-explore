# 隔离独立审查任务：A-TP-186-ROW1-0006

父任务#62。候选分支`attempt/preproject-twin-prime/186-row1-06`。
候选commit由父Issue最终冻结交接给出；各源码/结果文件哈希见artifact-sha256.json。
基线main为39002c5a6af8c7b7f093589e6a76cfd218fcbb99。

使用新的run和上下文，角色independent_verifier。只读冻结14文件、下载包bulk数据、
指定前驱档案及原始来源，不继承solver对话，不修改候选。此文件不表示审查已经执行。

## 验证目标

V1 输入身份：用户完整前驱ZIP SHA 8e6827feed5d06a9ec49552fa863b7c389ee502e23dbb8040b2bcd3b06f14f38；
不要把GitHub #59等同名但不同实现的包当成它。核对nested03/04哈希、边缘33列和版本锁。

V2 从Challenge.lean精确重建G0:R01：M3497、B4704、cap49152、theta140、L93607、U95638。
检查activation量词、下界39格修正、规格在95599后的更小cap被安全放大而非等同。
目标根2285、face577，公共单位I0/10^18。

V3 种子与完整核：核对640-bit renewal/指数包络的数学有效性，逐点常值密度支配而非
仅有质量就假设均匀；所有背景数0..28、指定见证未标记数0..27的Eulerian覆盖。
检查320-bit定向舍入和GMP无进位槽容量，不依赖仅通过小测试的叙述。

V4 共同正测度和平方：核对alpha/beta及Z_lower归一化固定，集合划分/子集重数，
53签名完整覆盖，带负号项用矩下端点，source face数40与见证位置数不丢失。

V5 关键前缀排除：本次全部三前缀界795.245299091不能认证577，必须证明cap支持关系
保证剩余配置前两个外壳都启用，只有前缀2/3。检查49152等号，边缘积分新变量不被误限。
三个前缀过粗的失败不得说成原积分违反目标。

V6 误差：独立导出见证数28、指数上界4、cover2268和E=6Eold<10^-77，保留profile因子。
检查tau10^-23及(1+tau)Q+(1+1/tau)E，不能沿用第一行较小E。

V7 重组准确分数，验证root<=2284.962701188<2285；face<=548.917567093<549<577。
root仅余0.037298813左右单位；计算区间窄不等于模型对物理对象的误差也窄。
验证整数face549和旧Young行预算仍为1；不宣称总损失改善。

V8 执行run_row.py于全新目录，和check_row.py正常/-O模式。检查168packing、196矩、
两见证signed-sum、8生产长度系数、196608种子端点、7坏参数、9旧行字段和53项重组。
检查生成数据与下载包哈希；elapsed_seconds可能不同，不要求两次日志时钟字节相同。

## Verdict

对精确限定命题给PASS/FAIL/INCONCLUSIVE，逐V1..V8记录最早错误或未闭合前提。
任何修复属于新的solver候选，不在本包修改。检查器结果不能自动证明物理测度输入。
即使PASS也不完成其余148项、全部有限域输入、完整恢复链、Lean公理消除或孪生素数猜想。
