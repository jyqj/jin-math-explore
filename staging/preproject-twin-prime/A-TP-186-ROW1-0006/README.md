# 第六轮：G0:R01 的实际全网格数值上界候选

**日期：**2026-09-06　**协调：**#62　**身份：**`A-TP-186-ROW1-0006`
**角色：**solver；未进行上下文隔离的独立数学审查。

## 结果

统一单位为 $I_0/10^{18}$，其中 $I_0=23685317816/10^{24}$。
以下小数是准确有理分数的向上舍入，不是浮点验收：

| 对象 | 本轮安全上界 | 固定目标 | 判定 |
|---|---:|---:|---|
| root | 2284.962701188 | 2285 | 条件数值认证候选通过 |
| face，含边缘数组误差 | 548.917567093 | 577 | 条件数值认证候选通过 |
| 更粗的全部三个前缀face | 795.245299091 | 577 | 这一上界不足，不能反推真实积分为假 |

root余量较小，约0.037298813单位。其有限支配模型求值区间宽度约9.12e-44单位。
face可收紧为整数549；但是原来的逐行10^-12粒度预算仍然是1。
本轮不宣称降低原表总预算或186素数间隔界。

## 与第一行不一样的地方

新行 M=3497，B=4704，theta=140，径向范围93607..95638，cap49152。
重新生成98,304个倾斜Dickman种子单元并完整检查196,608个端点；高片段数0..28全部纳入。
原试验、77个系数和完整有理边缘数组不变，但新核和边缘误差预算不能沿用旧行。
本行误差预算准确地为第一行的六倍，实际加进最终face上界。

第二行必须使用前缀1排除。源权非零时，剩余坐标的片段测度均支持于[0,49152h]，
故原试验前两个外壳的cap条件都满足，边缘只能选前缀2或3。等号端点也包括。
不能把边缘积分的新变量与被擦除的源坐标混为一谈。完整论证见 [proof.md](proof.md)。

## 已执行

一个完整生产过程：53类带符号平方签名、1,820次非平凡准确GMP卷积，记录总耗时
700.0042706009999秒。Python3.13.5，GMP6.3.0，320-bit定点求值；种子为640-bit。
库算术和实现属于信任边界，不等于Lean内核证明。

正常与Python -O检查结果逐字节一致。包括168个小卷积oracle、196个矩端点对照、
带符号root/face直接求和、8个生产长度卷积系数直接核对、9个旧行参数回归、
7个破坏行参数测试、完整种子重放和全部53项准确重组。
完整生产计算没有重复运行第二次；这些测试不构成上下文隔离的独立审查。

## 文件和复现

[results.json](results.json) 保存准确分数；[checks.json](checks.json) 保存测试结果；
[source-row.json](source-row.json)、[rounding-budget.json](rounding-budget.json) 绑定新行；
[verification-ticket.md](verification-ticket.md) 为独立审查任务。

依赖必须是用户本地完整第五轮ZIP，SHA-256：
`8e6827feed5d06a9ec49552fa863b7c389ee502e23dbb8040b2bcd3b06f14f38`。
这是ctypes/GMP版本，不从类似名称的GitHub #59包推断同一性。
下载本轮全量包后，在本目录运行（需要Python3.10+和可调用系统GMP）：

```sh
python3 -B run_row.py --predecessor dependencies/attempt05.zip --work reproduced-row1
python3 -B check_row.py --work reproduced-row1 --include-results
python3 -O -B check_row.py --work reproduced-row1 --include-results
```

work目录必须全新。程序无网络访问，不读取旧数值结果为新结论；小规模预检可用`--preflight-only`。
GitHub仅保存14个声明/源码/结果文件；数据数组、53项明细、日志和完整依赖在下载包中，
不得声称这些bulk文件已在远端。目录清单见[delivery-status.json](delivery-status.json)。

## 权威边界和后续

本轮数值上界依赖已明确列出的物理测度/Dickman/Mecke输入、边缘包络及其与实现的对应。
所有结果是solver候选，未执行Lean构建、原始FLINT程序、全仓库CI或隔离数学审查。
前两行四个标量上界候选不能代替全部152项，也不能证明孪生素数猜想。

下一solver目标是G0:R02，原表2285/577不再适用；该行theta有特殊值120，必须先重建参数。
本行保持冻结，独立审查尤其关注必要的前缀排除与很小的root余量。
