# Legacy Math Research Prompt compatibility notice

> [!CAUTION]
> Deprecated and inspection-only. Do not use this reference to draft a runnable Prompt v3, create a Goal, launch a process, request a hash echo, or change an existing run.

This path exists only to explain historical Prompt v3/v4 artifacts. Preserve such a run in its original `笔记草稿/数学研究运行/<run-id>` directory with the exact thread/executable, original Prompt and Continuation v1, counters, limits, and frozen `never` approval policy. Historical copies are read-only. Startup v3 does not infer or execute a Resume route from these files.

New contracts use [the Contract v8 template](contract-and-prompt-template-v8.md) and the direct-current-Goal-task protocol. Prompt v7, Startup Router v2, and launcher v2 remain legacy inspection labels. Never regenerate or mutate the old run. Contract v8 now implements an explicitly authorized `LEGACY_SUCCESSOR`: a user-created new active Goal may add a new v8 run in the same project, inheriting every legacy artifact, route, failure, audit, counter, and budget while preserving all old bytes; this is not legacy Goal recovery or Resume.

Legacy `rollover=false` / `rollover=never` blocks only automatic or same-run rollover. It does not block a user-explicit new Goal plus the prescribed additive v8 successor under the normalized same-target/nonexpanded research envelope, with all predecessor consumption inherited. Equality is research-semantic/resource/privacy/external-effect equivalence after strict hash-cross-bound recorded amendments; legacy HMAC fields are not authenticated. Mandatory retirement of old child-Goal/launcher/dispatcher/lease/control-receipt authority and replacement by the v8 current-Goal path is a safe contraction, not an expansion. Every other envelope difference remains planning-only/read-only pending an implemented `RUN_SUCCESSOR`; confirmation alone is not executable.
